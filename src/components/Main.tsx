import { Stack, StackProps } from '@chakra-ui/react'

export const Main = (props: StackProps) => (
  <Stack
    spacing="0rem"
    width="100%"
    maxWidth="100%"
    pt="2rem"
    px="0rem"
    {...props}
  />
)
